the side bar (for logout button), 
student-portal(for redirecting to login page when no username is found), 
login page (to save username inputted and navigate to portal page), 
Dashboard and student-top-bar (to display username stored) codes


-------they all have the local-storage and/or navigate-route 
functions, which i need to remove when i start using 
the real backend session create api